<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/play_music_bg" >
    
    <Button 
        android:id="@+id/musiclist_btn_sync"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center_vertical"
        android:background="@drawable/add_state"
        android:textColor="@color/white"
        android:text="@string/music_sync"/>

    <ListView 
        android:id="@+id/musiclist_lv_songs_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:fadingEdge="none"
        android:focusableInTouchMode="true"
        android:cacheColorHint="#00000000"
        android:layout_above="@id/musiclist_btn_sync"
        >
    </ListView>
    
</RelativeLayout>
